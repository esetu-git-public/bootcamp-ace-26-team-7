# Use CPU-only PyTorch index (HF Spaces has no GPU — wheels are ~120MB vs 532MB)
--extra-index-url https://download.pytorch.org/whl/cpu

# Backend
fastapi==0.139.0
uvicorn==0.50.0
supabase==2.31.0
python-multipart==0.0.32
pydantic==2.13.4
email-validator==2.3.0
python-dotenv==1.2.2
bcrypt==4.3.0

# Frontend
# streamlit is provided by HF Spaces Streamlit SDK — omit to avoid version conflicts

# ML / Data
torch==2.12.1
torchvision==0.27.1
Pillow==12.3.0
matplotlib==3.11.0
seaborn==0.13.2
scikit-learn==1.9.0
pandas==3.0.3
numpy==2.5.0
huggingface_hub==0.31.2
wandb
tqdm
